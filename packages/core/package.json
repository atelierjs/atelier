{
  "name": "@atelierjs/core",
  "version": "0.0.0",
  "main": "dist/index.js",
  "author": "Travis Henning & Ryan Meloy",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf dist",
    "build:compile": "gulp build",
    "build:copy": "cross-env mkdir -p dist/scss && cp -r src/** dist/scss",
    "build": "npm-run-all build:copy build:compile",
    "prewatch": "yarn build",
    "watch": "gulp watch"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "sass": "^1.52.1"
  }
}
